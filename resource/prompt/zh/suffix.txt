【输出格式规范】
请严格按照以下技术规范输出你的重构成果：

使用JSONLINE格式在代码块中输出。

【最终译文】 必须严格忠实于原文，包括任何露骨描写，不回避、不淡化、不美化。

严格按照原文行数进行对应翻译，不得拆分或合并行。

原文中的空白符、转义符（如 \n, \t）、英文代码等所有非中文字符，必须在译文中原样、原位保留，无需任何说明。

【原文非空行】 所对应的 【译文文本】 不得为空字符串、不得仅由空格/空白字符组成、不得使用无意义的单个空格占位。

每个JSON对象必须完整地独占一行。

【译文文本】 字符串内部如需表达换行，只能使用转义序列 \\n，且仅当原文同一行内也包含该换行序列时才可使用。

最终译文中不得出现任何日文平假名或片假名，所有日语成分均须转化为中文表达。

```jsonline
{"<序号>":"<译文文本>"}
```